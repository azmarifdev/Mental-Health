const Chat = () => {
    return (
        <section className="container py-10">
            <div className="max-w-2xl mx-auto rounded-xl bg-white p-6 shadow">
                <h1 className="text-2xl font-semibold mb-3">Chat With Bot</h1>
                <p className="text-gray-600">
                    Chat UI is not available in this repository snapshot yet.
                </p>
                <p className="text-gray-600 mt-2">
                    Backend chat endpoints are ready under <code>/user/chat-with-ai/:id</code>.
                </p>
            </div>
        </section>
    );
};

export default Chat;
